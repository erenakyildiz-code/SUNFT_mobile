import 'package:flutter/material.dart';

const containerColor = Color(0x2CFFFFFF);
var containerBoxDecoration = BoxDecoration(
  color: containerColor,
  borderRadius: BorderRadius.all(Radius.circular(10))


);